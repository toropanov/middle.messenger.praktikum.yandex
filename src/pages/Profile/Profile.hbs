<div class="wrapper">
  <div class="side_action">
    <a href="/chat" class="side_action__button">
      <
    </a>
  </div>

  <div class="profile">
    <div class="profile__wrap">
      <button class="profile__avatar">
        <img alt="avatar" src="{{user.avatar_url}}" alt="" class="avatar">
        <input type="file">
      </button>
      <div class="profile__fields">
        {{{inputs}}}
      </div>

      {{#if readOnly}}
        <a class="profile__link" href="/profile/edit">Изменить данные</a>
        <a class="profile__link" href="/auth/sign_in">Выйти</a>
      {{else}}
        <a class="profile__link" href="/auth/sign_in">Выйти</a>
      {{/if}}
    </div>
  </div>
</div>

