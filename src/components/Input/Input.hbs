    <input
      class="input__el {{class}}"
      name="{{name}}"
      defaultValue="{{value}}"
      type="{{type}}"
      id="{{name}}"
      value="{{value}}"
      placeholder="{{label}} пустой"
      title="Паттерн для ввода: {{pattern}}"
      {{#if minlength}}minlength={{minlength}}{{/if}}
      {{#if maxlength}}maxlength={{maxlength}}{{/if}}
      {{#if required}}required{{/if}}
      {{#if pattern}}pattern="{{pattern}}"{{/if}}
    />
